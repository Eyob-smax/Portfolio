# Base URL of the portfolio backend. Inlined into the bundle at build time —
# changing it requires a rebuild, not just a dev-server restart.
# Local backend: http://localhost:4000
VITE_API_BASE_URL=https://portfolio-backend-two-mocha.vercel.app
